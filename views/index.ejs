<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', {title: title}) %>
  <body>
    <%- include('partials/nav') %>
    <ul>
      <% messages.map((message) => { %>
      <li>
        <p>Id - <%= message.id%></p>
        <h3><%= message.text %></h3>
        <p><%= message.user %></p>
        <p><%= message.added %></p>
      </li>
      <%}) %>
    </ul>
    <%- include('partials/footer') %>
  </body>
</html>
